<header class="navheader">
  <!--<a href="/" class="logo">utdann.ME</a>-->
  <input class="menu-btn" type="checkbox" id="menu-btn" />
  <label class="menu-icon" for="menu-btn"><span class="navicon"></span></label>
  <ul class="menu">
        <li role="presentation" 
          @if (ViewData["active-link"] == "Home")
          {
              @: class="active"
          }><a href="/">Home</a></li>
        <li role="presentation"
        @if (ViewData["active-link"] == "Base")
          {
              @: class="active"
          }><a href="/Base64">Base64</a></li>
        <li role="presentation"
        @if (ViewData["active-link"] == "Html")
        {
            @: class="active"
        }><a href="/Html">Html</a></li>
        <li role="presentation"  
        @if (ViewData["active-link"] == "Url")
        {
            @: class="active"

        }><a href="/Url">Url</a></li>
        <li role="presentation"  
            @if (ViewData["active-link"] == "About")
            {
                @: class="active"
            }><a href="/About">About</a></li>
  </ul>
</header>