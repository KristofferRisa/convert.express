@page
@using Convert.Express.Pages;
@model HtmlModel
@{
    ViewData["Title"] = "Html";
    ViewData["active-link"] = "Html";
    Layout = "_Layout";
}

<div class="ui breadcrumb">
    <a class="section" href="/">Home</a>
    <span class="divider">/</span>
    <a class="section" href="#">HTML</a>
</div>

<h1 class="">HTML encode and decode</h1>

<form method="POST" action="~/Html" class="ui form" role="form">
    @Html.AntiForgeryToken()
    <div class="inline fields">
        <div class="field">
            <label>Encode:</label>
            <textarea name="encode" rows="3" required>@ViewData["Encode"]</textarea>
        </div>

        <div class="field">
            <label>Result:</label>
            <textarea rows="3">@ViewData["Encoded"]</textarea>
        </div>


    </div>
    <div class="field">
        <input type="submit" class="ui button" value="Convert!" />
    </div>

</form>


<div class="top-buffer">
    <form method="POST" action="~/Html" class="ui form" role="form">
         @Html.AntiForgeryToken()
        <div class="inline fields">
            <div class="field">
                <label>Decode:</label>
                <textarea name="decode" rows="3" required>@ViewData["Decode"]</textarea>
            </div>

            <div class="field">
                <label>Result:</label>
                <textarea rows="3">@ViewData["Decoded"]</textarea>
            </div>
        </div>


        <div class="field">
            <input type="submit" class="ui button" value="Convert!" />
        </div>



    </form>

</div>

